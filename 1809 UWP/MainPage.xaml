<Page
    x:Class="_1809_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_1809_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    
    mc:Ignorable="d">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1008" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="NavView.PaneDisplayMode" Value="Left"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Background="Transparent" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Wiki Viewer"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="16,0,0,0"
                       Grid.Column="1"
                       Style="{StaticResource CaptionTextBlockStyle}"/>
        </Grid>

        <muxc:NavigationView Grid.Row="1"
                             x:Name="NavView"
                             PaneDisplayMode="LeftCompact" 
                             IsSettingsVisible="True"
                             IsBackButtonVisible="Collapsed"
                             IsBackEnabled="False"
                             ItemInvoked="NavView_ItemInvoked"
                             Loaded="NavView_Loaded"
                             BackRequested="NavView_BackRequested"
                             IsTitleBarAutoPaddingEnabled="True">

            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem Content="Home" Tag="home" Icon="Home"/>
                <muxc:NavigationViewItem Content="Random Article" Tag="random" Icon="Shuffle"/>
                <muxc:NavigationViewItem Content="Favourites" Tag="favourites" Icon="Favorite"/>
                <muxc:NavigationViewItem x:Name="LoginNavItem" Content="Login" Tag="login" Icon="Contact"/>
            </muxc:NavigationView.MenuItems>

            <muxc:NavigationView.AutoSuggestBox>
                <AutoSuggestBox x:Name="SearchBox"
                                QueryIcon="Find"
                                PlaceholderText="Search..."
                                QuerySubmitted="SearchBox_QuerySubmitted"
                                TextChanged="SearchBox_TextChanged"/>
            </muxc:NavigationView.AutoSuggestBox>

            <Grid>
                <Frame x:Name="ContentFrame" Grid.Row="1" Navigated="ContentFrame_Navigated"/>

                <muxc:InfoBar x:Name="ConnectionInfoBar"
                              Grid.Row="0"
                              Title="Connection Issue"
                              Severity="Warning"
                              IsOpen="False"
                              IsClosable="False"
                              Margin="12,12,12,0"
                              VerticalAlignment="Top">
                    <muxc:InfoBar.ActionButton>
                        <Button x:Name="InfoBarButton" Content="Action" Click="InfoBarButton_Click" Visibility="Collapsed"/>
                    </muxc:InfoBar.ActionButton>
                </muxc:InfoBar>
            </Grid>
        </muxc:NavigationView>

        <muxc:WebView2 x:Name="ApiFetchWebView"
                       Width="1" Height="1" Opacity="0"
                       VerticalAlignment="Bottom" HorizontalAlignment="Right"
                       Grid.RowSpan="2"/>
        <Grid x:Name="WorkerWebViewHost" Visibility="Collapsed"/>
    </Grid>
</Page>