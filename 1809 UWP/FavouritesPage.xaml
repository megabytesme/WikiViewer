<Page
    x:Class="_1809_UWP.FavouritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Favourites" Style="{ThemeResource TitleTextBlockStyle}" Margin="24,12"/>

        <ListView x:Name="FavouritesListView"
                  Grid.Row="1"
                  IsItemClickEnabled="True"
                  ItemClick="FavouritesListView_ItemClick"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Style="{ThemeResource BodyTextBlockStyle}" Margin="12,8"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="NoFavouritesTextBlock"
                   Text="You haven't added any Favourites yet."
                   Grid.Row="1"
                   Style="{ThemeResource SubtitleTextBlockStyle}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>