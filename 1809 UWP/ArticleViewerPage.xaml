<Page
    x:Class="_1809_UWP.ArticleViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_1809_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ArticleTitle" 
                   Grid.Row="0"
                   Style="{ThemeResource HeaderTextBlockStyle}" 
                   TextWrapping="Wrap"
                   Margin="12,0,12,12"/>

        <xaml:WebView2 x:Name="ArticleDisplayWebView"
                       Grid.Row="1"
                       Margin="12,0,12,12"/>

        <xaml:WebView2 x:Name="SilentFetchView" Width="1" Height="1" Opacity="0" Grid.RowSpan="2" />

        <Grid x:Name="LoadingOverlay" Background="#4CFFFFFF" Visibility="Collapsed" Grid.RowSpan="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <ProgressRing IsActive="True" Width="60" Height="60"/>
                <TextBlock x:Name="LoadingText" Text="Loading..." HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>