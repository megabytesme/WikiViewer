<Page
    x:Class="_1809_UWP.ArticleViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Page.Resources>
        <AcrylicBrush x:Key="TransparentAcrylic"
                          BackgroundSource="Backdrop"
                          TintColor="{ThemeResource SystemAltHighColor}"
                          TintOpacity="0.4"
                          FallbackColor="{ThemeResource SystemAltHighColor}"/>

        <Storyboard x:Name="FadeInAnimation">
            <DoubleAnimation
                Storyboard.TargetName="LoadingOverlay"
                Storyboard.TargetProperty="Opacity"
                From="0" To="1" Duration="0:0:0.250"
                EnableDependentAnimation="True"/>
        </Storyboard>
        <Storyboard x:Name="FadeOutAnimation">
            <DoubleAnimation
                Storyboard.TargetName="LoadingOverlay"
                Storyboard.TargetProperty="Opacity"
                From="1" To="0" Duration="0:0:0.350"
                EnableDependentAnimation="True"/>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <muxc:WebView2 x:Name="ArticleDisplayWebView"
                       Grid.Row="0"
                       Grid.RowSpan="2"/>

        <Grid x:Name="TitleBarBackground"
              Grid.Row="0"
              VerticalAlignment="Top"
              SizeChanged="TitleBar_SizeChanged"
              Margin="0,0,15,0">

            <TextBlock x:Name="ArticleTitle" 
                       Style="{ThemeResource HeaderTextBlockStyle}" 
                       TextWrapping="Wrap"
                       Margin="12,12,12,12"/>
        </Grid>

        <CommandBar VerticalAlignment="Bottom" 
                    Grid.RowSpan="2" 
                    Background="{ThemeResource TransparentAcrylic}"
                    Margin="0,0,15,0">
            <AppBarButton x:Name="EditButton" 
                          Icon="Edit" 
                          Label="Edit" 
                          Click="EditButton_Click" 
                          Visibility="Visible"/>
        </CommandBar>

        <Grid x:Name="WorkerWebViewHost" Visibility="Collapsed"/>

        <Grid x:Name="LoadingOverlay" 
              Opacity="0" 
              IsHitTestVisible="False" 
              Grid.RowSpan="2"
              Background="{ThemeResource TransparentAcrylic}">

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <ProgressRing IsActive="True" Width="60" Height="60"/>
                <TextBlock x:Name="LoadingText" Text="Loading..." HorizontalAlignment="Center"/>
                <TextBlock x:Name="LastUpdatedText" Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.7" Visibility="Collapsed" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="VerificationPanel" Grid.RowSpan="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Margin="12" Spacing="8">
                <TextBlock Text="Please complete the security check" Style="{ThemeResource TitleTextBlockStyle}"/>
                <TextBlock Text="The app will continue automatically once you finish." Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap"/>
            </StackPanel>
            <muxc:WebView2 x:Name="VerificationWebView" Grid.Row="1" />
        </Grid>
    </Grid>
</Page>