<Page
    x:Class="_1809_UWP.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <Storyboard x:Name="FadeInWebView">
            <DoubleAnimation
                Storyboard.TargetName="EditWebView"
                Storyboard.TargetProperty="Opacity"
                From="0" To="1" Duration="0:0:0.350"
                EnableDependentAnimation="True"/>
        </Storyboard>
        <Storyboard x:Name="FadeOutOverlay">
            <DoubleAnimation
                Storyboard.TargetName="LoadingOverlay"
                Storyboard.TargetProperty="Opacity"
                From="1" To="0" Duration="0:0:0.350"
                EnableDependentAnimation="True"/>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Style="{StaticResource PageTitleBarGridStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="PageTitle" Style="{StaticResource PageTitleBarTextStyle}"/>
            <AppBarButton x:Name="CancelButton" Icon="Cancel" Label="Cancel" Click="Cancel_Click" 
                          VerticalAlignment="Center" Grid.Column="1"/>
        </Grid>

        <muxc:WebView2 x:Name="EditWebView" Grid.Row="1" Opacity="0" />

        <Grid x:Name="LoadingOverlay" Grid.RowSpan="2" Background="{StaticResource PageTitleBarAcrylicBrush}">
            <ProgressRing Width="60" Height="60" IsActive="True"/>
        </Grid>
    </Grid>
</Page>